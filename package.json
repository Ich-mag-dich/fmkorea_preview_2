{
  "name": "fmkorea-preview-2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:extension": "vue-tsc --noEmit && vite build --watch",
    "build:testbuild": "vue-tsc --noEmit && vite build",
    "build": "bestzip dist.zip dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.0",
    "bestzip": "^2.2.1",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vue-tsc": "^0.40.4"
  }
}
